<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alvin.sample">

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GFAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".def.DefaultActivity" />
        <activity android:name=".def.DefaultFragmentActivity" />
        <activity android:name=".custom.CustomActivity" />
        <activity android:name=".custom.CustomFragmentActivity" />
        <activity android:name=".local.LocalActivity" />
        <activity android:name=".local.LocalFragmentActivity" />
        <activity android:name=".sample.SampleActivity" />
        <activity android:name=".sample.SampleFragmentActivity" />
        <activity android:name=".vm.SampleVMActivity" />
    </application>

</manifest>